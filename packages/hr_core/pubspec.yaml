name: hr_core
description: Core domain and data layers for the HR application workspace.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  equatable: ^2.1.0
  injectable: ^3.0.0
  get_it: ^9.2.1

dev_dependencies:
  test: ^1.24.0
